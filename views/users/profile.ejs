<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <meta http-equiv="X-UA-Compatible" content="ie=edge">
  <title>Profile</title>
  <link href="https://fonts.googleapis.com/css?family=Raleway" rel="stylesheet">
  <link rel="stylesheet" href="/css/profileStyle.css">
  <script src="https://cdnjs.cloudflare.com/ajax/libs/jquery/3.1.1/jquery.min.js"></script>
  <script src="/js/profileScript.js" type="text/javascript"></script>
</head>
<body>

  <header>
    <div class="mic-wrapper" id="speak">
      <img src="https://lh4.ggpht.com/3YabEP2rKnKm7RpyGeU0ERPdzh4Ww9ZZ13Eu9_1TnH_b3gn_XK42GcWKkCfIq3GJo44=w300" alt="mic">
    </div>

    <div class="msg">
      <h1>Hi <%= user.username %>, what would you like to eat today?</h1>
    </div>

    <div class="logout">
      <form class="logout-form" action="/auth?_method=DELETE" method="post">
        <input type="submit" value="Logout" src="http://plainicon.com/dboard/userprod/2805_fce53/prod_thumb/plainicon.com-48720-512px-382.png">
      </form>
    </div>
  </header>

  <form id="hiddenForm" action="/search" method="POST"></form>

  <div class="fav">
    <% fav.forEach(result => { %>
      <div class="business-info">
        <form id="remove" action="/users/profile/<%= result._id %>?_method=DELETE" method="POST">
          <input type="submit" name="name" value="Remove">
        </form>

        <div class="name">
          <img class="business-img" src="<%= result.image_url %>" alt="businenss-img">
          <h3 clas="business-name"><%= result.name %></h3>
          <img src="<%= result.rating_img_url %>" alt="review-img">
        </div>
        
        <div class="info">
          <p class="business-reviews">Reviews: <%= result.review_count %></p>
          <p class="business-addr1"><%= result.display_addr %></p>
          <p class="business-phone">Phone: <%= result.phone %></p>

        </div>
      </div>
    <% }) %>
  </div>

</body>
</html>
