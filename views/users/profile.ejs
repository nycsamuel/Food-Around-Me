<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <meta http-equiv="X-UA-Compatible" content="ie=edge">
  <title>Profile</title>
  <link href="https://fonts.googleapis.com/css?family=Raleway" rel="stylesheet">
  <link rel="stylesheet" href="css/profileStyle.css">
  <script src="https://cdnjs.cloudflare.com/ajax/libs/jquery/3.1.1/jquery.min.js"></script>
  <script src="/js/profileScript.js" type="text/javascript"></script>
</head>
<body>

  <h1>Welcome! <%= user.username %></h1>
  <form action="/auth?_method=DELETE" method="post">
    <input type="submit" value="Logout">
  </form>


  <button id="speak">What are you in a mood for?</button>
  <form id="hiddenForm" action="/search" method="POST"></form>


  <div class="fav">
    <% fav.forEach(result => { %>
      <div class="business-info">
        <img class="business-img" src="<%= result.image_url %>" alt="businenss-img">
        <h3 clas="business-name"><%= result.name %></h3>
        <img src="<%= result.rating_img_url %>" alt="review-img">
        <p class="business-reviews"><%= result.review_count %></p>
        <p class="business-addr1"><%= result.display_addr %></p>
        <p class="business-phone"><%= result.phone %></p>

        <form action="/users/profile/<%= result._id %>?_method=DELETE" method="POST">
          <input type="submit" name="name" value="Remove">
        </form>
      </div>
    <% }) %>
  </div>

</body>
</html>
